name: Notify WeCom

on:
  push:
    branches: ['**']
  delete:
  pull_request:
    types: [opened, closed]

jobs:
  notify:
    uses: Einfo-Tech/.github/.github/workflows/wecom-notify.yml@main
    secrets: inherit
